let name = "Sebastian";
const name2: string = "Bruno";

let hpPoints: number | string = 600; //Se usa el pipe para decir que se pude usar number O string
const isAlive: Boolean = true

hpPoints = 'FULL'

console.log({
    name,
    hpPoints,
    isAlive
}

)

export {};